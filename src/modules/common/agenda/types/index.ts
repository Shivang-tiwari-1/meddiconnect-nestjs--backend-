export interface AvailabiltyType {
  day: string;
  start: string;
  end: string;
  data: string;
  laterNumber: {
    number: number;
  };
  available: boolean;
}

export interface Updatedate {
  day: string;
  start: string;
  end: string;
  date: string;
  laterNumber: {
    number: number;
  };
  available: boolean;
}
